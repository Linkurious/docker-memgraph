Memgraph has been deployed as an umbrella chart with two sub-charts:

  - Memgraph DB:  {{ include "memgraph.dbServiceName" . }}.{{ .Release.Namespace }}.svc (Bolt port {{ (index .Values "memgraph" "service" "boltPort") | default 7687 }})
  - Memgraph Lab: {{ include "memgraph.labServiceName" . }}.{{ .Release.Namespace }}.svc (port {{ (index .Values "memgraph-lab" "service" "port") | default 3000 }})

{{- if .Values.ingressRoute.enabled }}

Access Memgraph Lab via:
  https://{{ include "memgraph.defaultHostUrl" . }}
{{- end }}

Connect to Memgraph via Bolt:
  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "memgraph.dbServiceName" . }} 7687:{{ (index .Values "memgraph" "service" "boltPort") | default 7687 }}
